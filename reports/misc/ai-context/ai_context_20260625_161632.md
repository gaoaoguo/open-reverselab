# ReverseLab AI Task Context

- Time: 2026-06-25T16:16:32+08:00
- Task: 更新 GitHub 仓库 LING71671/open-reverselab 并配置 MCP
- Board filter: (auto)

## No-popup policy

- AI may run entries with launch_mode=cli or launch_mode=file_probe. Entries with launch_mode=gui must not be launched during automatic checks; verify them only by file existence/hash unless the user explicitly asks to open the GUI.
- PATH bootstrap: `$env:Path = "$(Resolve-Path tools/bin);$(Resolve-Path tools/ctf-website/bin);$env:Path"`

## Matched playbook rules

### jadx-corrupt-repair (score 1)

- When: GitHub release 下载不稳且本地 jadx zip/jar 损坏
- Tools: android.jadx
- Scripts: 
- Method: 若 jadx 再损坏，优先从 SourceForge mirror 下载 v1.5.5 zip，验证 zip/jar 后解压到 tools/android/jadx，并确认 ai_toolcheck android PASS。
- Evidence: Tool version output and analysis artifacts produced in the current workspace.

## Related practical findings

- No related finding found. If this task yields a reusable insight, record it with `ai_finding.py add`.
## Suggested tools

- `android.jadx` [android/cli/ai=True]: jadx CLI -> `tools\bin\jadx.bat`
  - Note: CLI wrapper uses absolute classpath and does not launch GUI.

## Callable tools in scope

- `android.apktool`: apktool -> `tools\bin\apktool.bat`
- `android.jadx`: jadx CLI -> `tools\bin\jadx.bat`
- `android.uber_apk_signer`: uber-apk-signer -> `tools\bin\uber-apk-signer.bat`

## If you discover something reusable

`python scripts\misc\ai_finding.py add --board <board> --kind <tactic|pitfall|tool-rule|dead-end|cve-chain|reversing-flow> --title "..." --trigger "..." --finding "..." --evidence "..." --reuse "..." --keyword ...`
